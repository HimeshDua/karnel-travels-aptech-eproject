@model List<TravelInfo>

@{
    ViewData["Title"] = "Travel Information";
}

<div class="page-header travel-header">
    <div class="header-overlay"></div>

    <div class="container header-content">
        <span class="header-eyebrow">Connectivity</span>

        <h1>
            Travel Smart with <span>Reliable Transport Info</span>
        </h1>

        <p class="header-subtitle">
            Get up-to-date information on trains, buses, flights,
            and travel routes to plan your journey efficiently.
        </p>

        <div class="header-meta">
            <span>🚆 Trains</span>
            <span>🚌 Buses</span>
            <span>✈️ Flights</span>
        </div>
    </div>
</div>


<div class="container">
    <section class="info-intro">
        <p>
            Find comprehensive information about various modes of transportation including trains, buses,
            flights, and ferries. Plan your journey with details about routes, schedules, and pricing.
        </p>
    </section>

    @if (Model != null && Model.Any())
    {
        <div class="items-grid">
            @foreach (var travel in Model)
            {
                <div class="item-card @(!travel.Availability ? "unavailable" : "")">
                    <div class="item-header">
                        <h3>@travel.Route</h3>
                        <span class="transport-type">@travel.TransportType</span>
                    </div>

                    <div class="item-details">
                        <p class="description">
                            @travel.Description
                        </p>
                        <p class="price">
                            <strong>💰 Fare:</strong> ₹@travel.Price.ToString("N0")
                        </p>
                        <p class="availability">
                            <strong>Status:</strong>
                            @if (travel.Availability)
                            {
                                <span class="badge badge-success">Available</span>
                            }
                            else
                            {
                                <span class="badge badge-closed">Not Available</span>
                            }
                        </p>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="no-data">
            <p>No travel information available at the moment. Please check back later.</p>
        </div>
    }

    <div class="back-link">
        <a asp-controller="Information" asp-action="Index">← Back to Information Hub</a>
    </div>
</div>
